import activityEntity from './activity.entity';

const ACTIVITY_ORDER_MAP = {
  id: activityEntity.id,
  name: activityEntity.name,
  created_at: activityEntity.createdAt,
};

export { ACTIVITY_ORDER_MAP };
